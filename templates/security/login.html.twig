{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4">Formulaire de connexion</h1>

        <form action="{{ path('app_login') }}" method ='post'>
            <div>
                <label for="username" class="form-label mt-4">Adresse Email </label>
                <input  value="{{ last_username }}" type="email" class="form-control" id="username" placeholder="example@symrecipe.com" name="_username">
            </div>
            <div>
                <label for="password" class="form-label mt-4">Mot de passe</label>
                <input type="password" class="form-control" id="password" name="_password" placeholder="*******">
            </div>
            <button type="submit" class="btn btn-primary mt-4">
                Se connecter
            </button>

        </form>
    </div>

{% endblock %}
